# מקום הפעילויות — Activities

התיקייה הזו מכילה את כל הפעילויות והמשחקים החינמיים של StoryLeap.
כל פעילות יושבת בתת-תיקייה משלה, עם README משלה שמסביר איך היא עובדת.

---

## העיקרון המנחה

**נבנה פעם אחת, עובד תמיד.**

כל פעילות כאן חייבת לעמוד בשלושת התנאים האלה:

| תנאי | למה |
|---|---|
| **אפס קריאות AI** | הפעילות לא שולחת בקשה לשום מודל, לא בזמן טעינה ולא תוך כדי משחק |
| **אפס backend** | אין קריאות רשת, אין entities, אין functions. הכול רץ בדפדפן |
| **תוכן כתוב מראש** | כל טקסט שהילד רואה נכתב ידנית לקובץ תוכן ונשמר בגיט |

התוצאה: הפעילויות עובדות אופליין, לא עולות כסף לכל שימוש, לא נשברות כשמודל מתעדכן,
ולא דורשות הרשמה. הן גם ניתנות לבדיקה מלאה — אותה קלט תמיד נותן אותו פלט.

זה **בכוונה** ההפך ממנוע הסיפורים בתשלום, שכן מייצר תוכן פר-ילד.

---

## מאיפה הפעילויות מגיעות

הפעילויות נבחרות מתוך `StoryLeap_Professional_Tools_Map_v2` — מיפוי של 60 כלים מקצועיים
(`02_Product / Professional_Tools_Suite`). כרגע אנחנו בונים מ**גל 0** בלבד, ומתוכו רק
כלים שעומדים בעיקרון למעלה.

מתוך 13 כלי גל 0, **12 עומדים בקריטריון**. היחיד שלא הוא *סיפור חברתי*, שרץ על
מנוע הסיפור/תרחיש ולכן דורש AI — הוא שייך למוצר בתשלום, לא לכאן.

12 הכלים הנותרים מתחלקים לארבעה מנועים:

| מנוע | כלים בגל 0 | סטטוס |
|---|---|---|
| כרטיסים | גלגל רגשות, קלפי חוזקות, Coping Cards, לוח בחירה, כרטיס בקשת הפסקה, קלפי רגשות | **5 מתוך 6 נבנו** |
| סולם/מדידה | מד חום רגשי, Emotion Check-in | **1 מתוך 2 נבנה** |
| קנבס יצירתי | ציור הרגש, מפת גוף | **1 מתוך 2 נבנה** |
| רצף וסדר יום | לוח סדר יום חזותי, First–Then | **2 מתוך 2 — הושלם** |

כל ארבעת המנועים נפתחו. נשארו בגל 0 **3 כלים**, כולם בתוך מנועים קיימים:

| כלי | הערכה |
|---|---|
| מפת גוף | שווה לבנות — האינטראקציה שונה מכל מה שיש |
| קלפי רגשות | חופף לגלגל הרגשות ולחוקר הרגשות |
| Emotion Check-in | חופף למד החום, ובלי שמירה "צ'ק-אין יומי" לא זוכר כלום |

**גל 1 מכיל עוד כ-8 כלים ללא AI** על אותם מנועים: Task Analysis,
רצף ADL חזותי, Checklist שגרה, טיימר חזותי, מקום בטוח, Visual Rules,
Calm Corner Kit, תרגילי נשימה.

> **הערה אסטרטגית.** מסמך האפיון מגדיר את Phase 0 כ**גילוי לפני בנייה**,
> ומסמן "Overexpansion" כסיכון המרכזי. עשר פעילויות עובדות הן כבר לא
> "דמואים לראיונות" אלא מוצר. בשלב מסוים האילוץ מפסיק להיות
> *מה לבנות* ומתחיל להיות *האם מישהו משתמש*.

---

## מה קיים כרגע

| פעילות | תיקייה | נתיב | מנוע | השאלה שהיא שואלת |
|---|---|---|---|---|
| למה אני מרגיש/ה ככה? | `feelings-explorer/` | `/activities/feelings-explorer` | דריל-דאון | **למה** אני מרגיש? |
| גלגל הרגשות | `emotion-wheel/` | `/activities/emotion-wheel` | כרטיסים · `spin` | מה אני מרגיש? |
| קלפי החוזקות שלי | `strength-cards/` | `/activities/strength-cards` | כרטיסים · `pick` | מי אני? |
| הקלפים שעוזרים לי | `coping-cards/` | `/activities/coping-cards` | כרטיסים · `build-a-kit` | מה אני עושה? |
| ציור הרגש | `emotion-drawing/` | `/activities/emotion-drawing` | קנבס | איך זה נראה? |
| מד החום של הרגשות | `emotion-thermometer/` | `/activities/emotion-thermometer` | סולם | כמה זה חזק? |
| לוח סדר היום שלי | `routine-board/` | `/activities/routine-board` | רצף | מה קורה היום? |
| קודם, ואז | `first-then/` | `/activities/first-then` | רצף | מה עכשיו ומה אחר כך? |
| לוח הבחירה | `choice-board/` | `/activities/choice-board` | כרטיסים | מה אני בוחר? |
| כרטיס ההפסקה שלי | `break-card/` | `/activities/break-card` | כרטיסים | איך אני אומר שאני צריך? |

כולן ללא AI, ללא backend, ומדפיסות פלט שאפשר לתלות.

### כיסוי אשכולות

| אשכול | פעילויות |
|---|---|
| זיהוי, ביטוי וויסות רגשי | 6 |
| הכנה, שגרה ומעברים | 2 |
| עיבוד רגשי וטיפול נרטיבי | 1 |
| עצמאות ותפקוד יומיומי | 1 |
| תקשורת חברתית, CBT, מדידה, הדרכת הורים | 0 |

**גיוון האינטראקציה נשמר בכוונה.** שלוש הראשונות נבנו ברצף וכולן
"בוחרים מתוך רשימה". ציור הרגש היה הפעילות הראשונה שבה הילד **יוצר**
במקום לבחור, מד החום הראשונה שבה הוא **מודד**, ולוח סדר היום הראשונה
שבה הוא **מסדר**. כשמוסיפים פעילות חדשה שווה לשאול לא רק
"איזה כלי חסר" אלא "איזו *פעולה* חסרה".

אותו כלל תפס גם ברמת האשכול: שש הפעילויות הראשונות עסקו כולן ברגשות,
ולוח סדר היום נבחר בדיוק כי הוא פתח את "הכנה, שגרה ומעברים" —
האשכול שהמוצר בתשלום כבר מדבר עליו (כיתה א', מעבר דירה) ולא היה מיוצג כאן כלל.

---

## מבנה התיקיות

```
src/components/activities/
├── README.md                       ← הקובץ הזה
├── shared/                         ← מה שיותר מפעילות אחת משתמשת בו
│   ├── README.md
│   ├── cardStyles.js               ← שכבת הכרטיסים
│   ├── ActivityCards.jsx
│   └── routineSteps.js             ← 28 שלבי שגרה, לשתי פעילויות הרצף
├── emotion-wheel/
│   ├── README.md                   ← איך גלגל הרגשות עובד
│   ├── emotionWheelContent.js      ← כל התוכן
│   └── EmotionWheel.jsx            ← הרכיב
├── strength-cards/
│   ├── README.md
│   ├── strengthCardsContent.js
│   └── StrengthCards.jsx
├── coping-cards/
│   ├── README.md
│   ├── copingCardsContent.js
│   └── CopingCards.jsx
├── emotion-drawing/
│   ├── README.md
│   ├── emotionDrawingContent.js
│   └── EmotionDrawing.jsx
├── emotion-thermometer/
│   ├── README.md
│   ├── emotionThermometerContent.js
│   └── EmotionThermometer.jsx
├── feelings-explorer/
│   ├── README.md
│   ├── feelingsExplorerContent.js
│   └── FeelingsExplorer.jsx
├── routine-board/
│   ├── README.md
│   ├── routineBoardContent.js
│   └── RoutineBoard.jsx
├── first-then/
│   ├── README.md
│   ├── firstThenContent.js
│   └── FirstThen.jsx
├── choice-board/
│   ├── README.md
│   ├── choiceBoardContent.js
│   └── ChoiceBoard.jsx
└── break-card/
    ├── README.md
    ├── breakCardContent.js
    └── BreakCard.jsx
```

`shared/` משרת את פעילויות הכרטיסים. ציור הרגש ומד החום משתמשים ממנו
רק ב-`PickerCard` למסך בחירת הרגש; "למה אני מרגיש/ה ככה?" לא משתמשת בו כלל.

הרכיבים יושבים כאן. **העמודים** יושבים ב-`src/pages/` לפי הקונבנציה של הפרויקט,
והם דקים בכוונה — כותרת, קישור חזרה, טיפ להורה, והרכיב עצמו.

---

## ארבע נקודות החיווט

כל פעילות מחוברת לאתר בארבעה מקומות. חשוב לעבור על כולם, אחרת הפעילות קיימת אבל לא נגישה:

1. **`src/components/activities/<שם>/`** — הרכיב וקובץ התוכן
2. **`src/pages/Activity<שם>.jsx`** — עמוד דק שעוטף את הרכיב
3. **`src/App.jsx`** — `import` + `<Route path="/activities/<שם>" …>`
4. **`src/pages/Activities.jsx`** — רשומה במערך `GAMES`, כדי שהכרטיס יופיע ב-hub

> `src/pages.config.js` הוא קובץ שנוצר אוטומטית. **לא נוגעים בו** — הנתיבים של הפעילויות
> מוגדרים ידנית ב-`App.jsx`, כמו שאר הנתיבים המותאמים באתר.

---

## איך מוסיפים פעילות חדשה

### 1. קובץ תוכן

`src/components/activities/<שם>/<שם>Content.js`. שלושה ייצואים קבועים:

```js
export const <DECK> = [ … ];   // הנתונים של הפעילות (קלפים, רגשות, שלבים)
export const UI = { he: {…}, en: {…} };     // כל מחרוזת שמוצגת למשתמש
export const META = { he: {…}, en: {…} };   // title + description ל-SEO
```

כל פריט בחפיסה נושא את הטקסט שלו בשתי השפות, כדי שהוספת פריט תהיה שינוי במקום אחד:

```js
{
  id: 'brave',
  emoji: '🦁',
  he: { label: 'אני אמיץ/ה גם כשמפחיד', prompt: 'מה עשית למרות שקצת פחדת?' },
  en: { label: 'I am brave even when it is scary', prompt: 'What did you do even though you were a bit scared?' },
}
```

### 2. הרכיב

מקבל `lang` כ-prop (`'he'` או `'en'`) — הוא לא קורא ל-`useLanguage` בעצמו.
זה משאיר אותו נקי מהקשר, וקל לבדיקה ולשימוש חוזר.

```jsx
export default function MyActivity({ lang = 'he' }) {
  const copy = UI[lang] || UI.he;
  …
}
```

### 3. העמוד

העמוד הוא זה שקורא ל-`useLanguage`, ומעביר את התוצאה פנימה:

```jsx
const { lang } = useLanguage();
const isHe = lang === 'he';
…
<MyActivity lang={isHe ? 'he' : 'en'} />
```

### 4. חיווט

`App.jsx` + `GAMES` ב-`Activities.jsx`. ראו למעלה.

### 5. README

תת-תיקייה בלי README היא תיקייה שאף אחד לא יבין בעוד חצי שנה. ראו את שתי הקיימות כדוגמה.

---

## קונבנציות

### שפה

דו-לשוני תמיד, עמוד אחד לשתי השפות. מתג השפה בהדר מחליף בין `he` ל-`en`
ללא ניווט. ה-RTL מטופל ברמת ה-`Layout` (`dir` על ה-shell), אז אין צורך ב-`dir`
ברכיב עצמו — אבל **כן** צריך להשתמש ב-`inset-inline-start/end` במקום `left/right`
לכל מיקום אבסולוטי, אחרת פינות וסמלים יקפצו לצד הלא נכון באנגלית.

### עיצוב

הפלטה של המותג:

| תפקיד | ערך |
|---|---|
| ורוד | `#FF6FB5` |
| תכלת | `#4FC3E8` |
| כחול כהה (טקסט/כותרות) | `#1A1A6E` |
| גבול רך | `#EDE9F8` |
| ורוד רקע | `#FFF0F7` / `#FFD6EC` |
| גרדיאנט כפתור ראשי | `linear-gradient(135deg, #FF6FB5, #4FC3E8)` |

הטיפ להורה בסוף כל פעילות משתמש בסכמה הצהובה (`#FFF8EC` / `#F5C842` / `#7A5000`),
כמו בפעילויות החינמיות הקיימות.

### נגישות

- `aria-pressed` על כל דבר שנבחר
- `aria-live="polite"` על אזור שמתעדכן אחרי פעולה (תוצאה, מונה)
- `prefers-reduced-motion` — כל אנימציה חייבת מסלול חלופי בלי תנועה
- מטרות מגע של 44px ומעלה. הקלפים והכפתורים עומדים בזה בנוחות

### הדפסה

`Layout.jsx` כבר מגדיר `@media print` שמסתיר `.site-chrome` ו-`.site-decor`
ומנקה את רקע האתר. פעילות שמייצרת פלט להדפסה צריכה רק:

- לסמן ב-`site-chrome` את מה שלא אמור להיות מודפס (כפתורים, קישור חזרה, טיפ להורה)
- להוסיף `break-inside: avoid` לפריטים שאסור שייחתכו באמצע
- לקרוא ל-`window.print()`

---

## על שכבת `shared/`

השכבה המשותפת **לא** נבנתה עם הפעילות הראשונה, ובכוונה.

מנוע הכרטיסים פותח 6 מכלי גל 0 ו-12 מתוך כל 60, ולכן היה מפתה להפשיט אותו מיד.
אבל מנוע שנגזר ממימוש אחד הוא ניחוש לגבי מה יחזור. במקום זה הופרד התוכן
מהרכיב בכל פעילות, וחיכינו לשלושה מימושים שונים באמת — `spin`, `pick`
ו-`build-a-kit` — ורק אז נגזר מהם מה שבאמת חזר.

מה שעבר: מראה הקלף, קלף התוצאה, שדה התשובה שמדפיס נכון, סרגל הפעולות.
מה שנשאר בכל פעילות: התוכן, וההתנהגות בלחיצה.

**הכלל:** הצורה משותפת, ההתנהגות לא. ראו [`shared/README.md`](shared/README.md).

אותו כלל תקף קדימה — פעילות בסולם או ברצף לא צריכה לדחוף את עצמה
לתוך `shared/` הקיים. אם תיווצר שכבה משותפת שנייה, שתיגזר גם היא
משלושה מימושים אמיתיים ולא מאחד.
